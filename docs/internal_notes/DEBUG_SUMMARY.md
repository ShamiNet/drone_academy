# 🔍 سجل Debug لإنشاء التقرير الشامل

## الخطوات الأساسية في العملية:

```
1️⃣ اختيار المتدربين
   └─ جلب قائمة المتدربين من Firebase

2️⃣ اختيار اللغة والإعدادات
   ├─ اللغة: ar / en / ru
   ├─ الذكاء الاصطناعي: تفعيل/تعطيل
   └─ العلامة المائية: تفعيل/تعطيل

3️⃣ جلب البيانات
   ├─ التدريبات
   ├─ نتائج التدريب
   ├─ الملاحظات اليومية
   └─ بيانات المستخدمين

4️⃣ تحليل الذكاء الاصطناعي (إذا فُعّل)
   └─ معالجة ملاحظات كل متدرب

5️⃣ معالجة بيانات المتدربين
   └─ حساب الإتقان والمستويات

6️⃣ اختيار الخط بناءً على اللغة
   ├─ العربية: Cairo
   └─ إنجليزي/روسي: Roboto

7️⃣ بناء وثيقة PDF
   ├─ إضافة صفحات للمتدربين
   ├─ تعيين اتجاه النص (RTL/LTR)
   └─ إضافة العلامات المائية

8️⃣ عرض نافذة النجاح
   └─ حفظ أو طباعة PDF
```

---

## 📍 أماكن الـ Debug Logs في الكود:

### 📄 `report_generation_dialogs.dart`:
```dart
// السطر 22-26: بدء العملية
// السطر 293-297: اختيار اللغة والإعدادات
// السطر 329-406: جلب ومعالجة البيانات
// السطر 498-504: تحليل الذكاء الاصطناعي
```

### 🎨 `pdf_generator.dart`:
```dart
// السطر 135-145: بناء المستند
// السطر 156-162: تحميل الخط والشعار
```

---

## 🎯 الكلمات المفتاحية للبحث في Logcat:

| البحث | الغرض |
|------|-------|
| `🔵` | بدايات العمليات |
| `✅` | العمليات الناجحة |
| `⚠️` | التحذيرات |
| `❌` | الأخطاء |
| `المتدربين:` | عدد المتدربين المختارين |
| `اللغة:` | اللغة المختارة |
| `تم جلب` | البيانات التي تم جلبها |
| `إضافة صفحة` | صفحات PDF قيد الإضافة |

---

## 🚀 للبدء بسهولة:

1. **افتح Logcat** في Android Studio أو VS Code
2. **ابدأ بتشغيل التطبيق**:
   ```bash
   flutter run -v
   ```
3. **اضغط على "إنشاء تقرير شامل"**
4. **راقب الرسائل** وتابع سير البرنامج

---

## 💡 نصيحة سريعة:

اضبط فلتر Logcat على كلمة `drone` أو `print` لرؤية فقط رسائلك:

```
adb logcat | grep -E "🔵|✅|❌|⚠️"
```
