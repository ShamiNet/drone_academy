# ميزة تغيير اللغة في التطبيق

## نظرة عامة
تم إضافة ميزة تغيير لغة التطبيق بنجاح في 4 صفحات رئيسية:

1. ✅ **صفحة الإعدادات** (AdminSettingsScreen)
2. ✅ **صفحة البروفايل** (ProfileScreen)  
3. ✅ **صفحة تسجيل الدخول** (LoginScreen)
4. ✅ **صفحة إنشاء الحساب** (SignupScreen)

## اللغات المدعومة
- 🇸🇦 **العربية** (ar)
- 🇬🇧 **الإنجليزية** (en)
- 🇷🇺 **الروسية** (ru)

## الملفات المضافة

### 1. خدمة إدارة اللغة
**المسار:** `lib/services/language_service.dart`

**الوظائف:**
- `saveLanguage(String)`: حفظ اللغة المختارة
- `getSavedLanguage()`: جلب اللغة المحفوظة
- `getSavedLocale()`: جلب اللغة كـ Locale
- `getLanguageNameInArabic(String)`: الحصول على اسم اللغة بالعربية
- `getLanguageNativeName(String)`: الحصول على اسم اللغة بلغتها

### 2. واجهة اختيار اللغة
**المسار:** `lib/widgets/language_selector.dart`

**المعاملات:**
- `showTitle`: إظهار عنوان "اختر اللغة" (افتراضي: true)
- `isCompact`: عرض مضغوط (dropdown) أو كامل (قائمة) (افتراضي: false)
- `onLanguageChanged`: دالة callback عند تغيير اللغة (اختياري)

**الاستخدام:**
```dart
// عرض كامل مع عنوان (للإعدادات والبروفايل)
const LanguageSelector(showTitle: true, isCompact: false)

// عرض مضغوط بدون عنوان (للتسجيل والدخول)
const LanguageSelector(showTitle: false, isCompact: true)
```

## التعديلات على main.dart

### تحميل اللغة عند بدء التطبيق
```dart
@override
void initState() {
  super.initState();
  _loadSavedLanguage();
}

Future<void> _loadSavedLanguage() async {
  final savedLocale = await LanguageService.getSavedLocale();
  if (savedLocale != null && mounted) {
    setState(() {
      _locale = savedLocale;
    });
  }
}
```

## كيف يعمل النظام؟

1. **اختيار اللغة:** المستخدم يختار اللغة من أي من الصفحات الأربع
2. **الحفظ:** يتم حفظ اختيار اللغة في SharedPreferences
3. **التطبيق الفوري:** يتم تغيير لغة التطبيق مباشرة
4. **الاستمرارية:** عند إعادة فتح التطبيق، يتم تحميل اللغة المحفوظة تلقائياً

## مواقع ظهور اختيار اللغة

### 1. صفحة الإعدادات
- موقع: في أعلى الصفحة
- نوع العرض: كامل مع أيقونات وأعلام
- مثالي لـ: المستخدمين الحاليين

### 2. صفحة البروفايل
- موقع: تحت الصورة الشخصية
- نوع العرض: كامل مع أيقونات وأعلام
- مثالي لـ: التخصيص الشخصي

### 3. صفحة تسجيل الدخول
- موقع: أسفل الصفحة بعد زر "إنشاء حساب"
- نوع العرض: مضغوط (dropdown)
- مثالي لـ: المستخدمين الجدد

### 4. صفحة إنشاء الحساب
- موقع: أسفل الصفحة بعد زر "التسجيل"
- نوع العرض: مضغوط (dropdown)
- مثالي لـ: التسجيل بلغة مريحة

## المميزات

✅ **حفظ تلقائي:** اللغة المختارة تُحفظ وتُستخدم في كل مرة  
✅ **تغيير فوري:** التطبيق يتغير مباشرة بدون إعادة تشغيل  
✅ **واجهة جميلة:** أيقونات وأعلام لكل لغة  
✅ **رسالة تأكيد:** إشعار أخضر يظهر عند تغيير اللغة  
✅ **تكامل كامل:** يعمل مع نظام الترجمة الموجود (l10n)  

## ملاحظات تقنية

### التخزين
يستخدم النظام `shared_preferences` لحفظ اختيار اللغة بشكل دائم.

### الأداء
- لا يؤثر على سرعة التطبيق
- تحميل اللغة يتم مرة واحدة عند البدء
- التغيير الفوري يستخدم setState فقط

### الصيانة
- جميع الملفات موثقة بشكل جيد
- الكود قابل للتوسع لإضافة لغات جديدة
- سهل التخصيص والتعديل

## إضافة لغة جديدة (مستقبلاً)

1. أضف Locale في `LanguageService.supportedLocales`
2. أضف اسم اللغة في `LanguageService.languageNames`
3. أضف علم اللغة في `_getLanguageFlag()`
4. أضف ملف .arb للترجمة في `lib/l10n/`

---

## مثال على الاستخدام في كود مخصص

```dart
import 'package:drone_academy/widgets/language_selector.dart';

// في أي صفحة تريد إضافة اختيار اللغة:
class MyCustomScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          // اختيار اللغة بعرض كامل
          LanguageSelector(
            showTitle: true,
            isCompact: false,
            onLanguageChanged: (locale) {
              print('تم تغيير اللغة إلى: ${locale.languageCode}');
            },
          ),
        ],
      ),
    );
  }
}
```

---

## المطور
تم تطوير هذه الميزة بواسطة: GitHub Copilot
التاريخ: 4 يناير 2026
